metadata:
  labels:
    some-label: java
spec:
  containers:
    - name: java
      image: openjdk:8-alpine
      command:
        - cat
      tty: true
      volumeMounts:
        - mountPath: /root/.gradle
          name: gradle
  volumes:
    - name: gradle
      hostPath:
        path: /data/cache/gradle/
        type: DirectoryOrCreate
